# Brewfile.work.example.tmpl
# This is a TEMPLATE for work-specific Brewfile
# Uses chezmoi template variables (e.g., {{ .git.gitlab_domain }})
#
# On work machines, this template is automatically rendered to ~/.Brewfile.work
# The setup script will:
# 1. Substitute template variables from ~/.config/chezmoi/chezmoi.toml
# 2. Create ~/.Brewfile.work (NOT in chezmoi, NOT committed to git)
# 3. Install from both ~/.Brewfile and ~/.Brewfile.work
#
# IMPORTANT: ~/.Brewfile.work should NEVER be added to chezmoi or committed to git

# ============================================================================
# TAPS - Work-Specific Homebrew Repositories
# ============================================================================

{{- if .git.gitlab_domain }}
# Work-specific tap from GitLab (uses gitlab_domain from chezmoi.toml)
tap "tools/core", "git@{{ .git.gitlab_domain }}:tools/homebrew-core.git"
{{- end }}

# ============================================================================
# WORK-SPECIFIC CLI TOOLS
# ============================================================================

# Authentication & Access
brew "kubectl-login"              # kubectl credential plugin for work clusters
brew "ssh-login"                  # SSH login helper for work infrastructure

# Work-specific cloud/k8s tools can go here
# brew "your-company-cli"         # Company-specific CLI tools

# ============================================================================
# WORK-SPECIFIC GUI APPLICATIONS
# ============================================================================

# Security & Authentication
cask "vip-access"                 # Symantec VIP Access for 2FA

# ============================================================================
# WORK-SPECIFIC MAC APP STORE APPS
# ============================================================================

mas "Okta Verify", id: 490179405  # Work SSO authentication
mas "Windows App", id: 1295203466 # Remote desktop for work Windows machines

# ============================================================================
# OPTIONAL: Add any other work-specific tools below
# ============================================================================

# Examples:
# brew "awscli"                    # If your company uses AWS
# cask "microsoft-teams"           # If required for work
# cask "slack"                     # Work Slack workspace
